syntax = "proto3";

package rank;
option go_package = "./;rank";

message RankSnapshot {
  map<string,uint32> snapshotData = 1; // id - > score
}

message RankItem {
  string id = 1;
  int64 score = 2;
  uint32 rank = 3;
  uint32 oldRank = 4;
}
